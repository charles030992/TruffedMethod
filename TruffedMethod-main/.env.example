# Example environment variables (DO NOT commit your real .env to git)
# Copy this file to .env and fill with real values locally

# Vite requires vars to start with VITE_ to be exposed to the client code
VITE_SEPOLIA_RPC_URL="https://eth-sepolia.g.alchemy.com/v2/<YOUR_ALCHEMY_KEY>"

# Private key used by Hardhat deploy scripts (only for local/CI; never commit)
# DEPLOYER_PRIVATE_KEY="<YOUR_PRIVATE_KEY>"
